export default class route {
    constructor(url, title, pathHtml, pthJS ="") {
        this.url = url;
        this.title = title;
        this.pathHtml = pathHtml;
        this.pthJS = pthJS;
    }
}